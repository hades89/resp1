/**
 * 
 */
package hello;

import org.springframework.web.util.UriComponents;

/**
 * @author QJ
 *
 */
public class UrlBuilder extends UriComponents{

	/**
	 * @param scheme
	 * @param fragment
	 */
	protected UrlBuilder(String scheme, String fragment) {
		super(scheme, fragment);
	}

}
